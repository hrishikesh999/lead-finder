name: Run Prospect Qualifier

on:
  workflow_dispatch:
    inputs:
      sample_size:
        description: "Rows to sample from the CSVs (default: 500)"
        required: false
        default: "500"
        type: string
      include_org:
        description: "Include .org domains (excluded by default)"
        required: false
        default: false
        type: boolean

# Before triggering: upload activecampaign.csv and keap.csv to the Google Drive
# folder whose ID is stored in the GOOGLE_DRIVE_FOLDER_ID secret. The script
# downloads them automatically at runtime — no files need to be committed.

jobs:
  qualify-prospects:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install dependencies
        run: uv sync

      - name: Run prospect qualifier
        run: |
          uv run python qualify.py \
            --sample-size ${{ inputs.sample_size }} \
            ${{ inputs.include_org == 'true' && '--include-org' || '' }}
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
          GOOGLE_SHEETS_CREDENTIALS_JSON: ${{ secrets.GOOGLE_SHEETS_CREDENTIALS_JSON }}
          GOOGLE_DRIVE_FOLDER_ID: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID }}
          HUNTER_API_KEY: ${{ secrets.HUNTER_API_KEY }}
