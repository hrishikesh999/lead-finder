# ── qualify.py — BuiltWith Prospect Qualifier ──────────────────────────────

# Anthropic API key (console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-...

# Google Sheets — the spreadsheet ID from the sheet URL
# e.g. https://docs.google.com/spreadsheets/d/THIS_PART_HERE/edit
GOOGLE_SHEET_ID=1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgVE2upms

# Google Drive folder ID containing activecampaign.csv and keap.csv
# Find it in the folder URL: drive.google.com/drive/folders/THIS_PART_HERE
# Share the folder with your service account email before use.
# Leave blank to read CSVs from the local input/ folder instead.
GOOGLE_DRIVE_FOLDER_ID=

# Google service account credentials JSON (entire JSON blob as a single-line string)
# 1. Google Cloud Console → IAM → Service Accounts → Create
# 2. Enable Google Sheets API + Google Drive API
# 3. Create a JSON key and download it
# 4. Minify: python -c "import json,sys; print(json.dumps(json.load(open('creds.json'))))"
# 5. Paste the minified result below (no newlines)
GOOGLE_SHEETS_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# --- Optional tuning (defaults shown) ---

# Sheet tab name where prospects are written
PROSPECT_TAB_NAME=Prospects

# Folder containing activecampaign.csv and keap.csv
INPUT_FOLDER=input

# How many CSV rows to sample per run
DEFAULT_SAMPLE_SIZE=500

# Concurrent homepage fetches (too high → sites refuse connections)
MAX_FETCH_CONCURRENCY=10

# Concurrent Claude Haiku calls
MAX_CLAUDE_CONCURRENCY=5


# ── Legacy prospect-finder variables (not used by qualify.py) ───────────────

YOUTUBE_API_KEY=your_youtube_api_key_here
HUNTER_API_KEY=your_hunter_api_key_here
NEON_DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require
MIN_YOUTUBE_SUBSCRIBERS=10000
MIN_EMAIL_CONFIDENCE=80
MAX_CANDIDATES_PER_RUN=400
ALLOWED_COUNTRIES=US,CA
CLAUDE_MODEL=claude-haiku-4-5-20251001
LOG_LEVEL=INFO
